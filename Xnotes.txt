//Routes endpoints URL

//1)express 2)app = express 3)server listens toPORT 4)app.get()...

// http method= get,post,put,patch,delete

to connect mogodb through mongoose
mongo <---mongoose<--Application

//Global error Handler
                                 next()
client-->Router-->Middleware-->multimiddleware-->requestHandler--|
                                                  |
                                        Response <-


now for the loacal host we will get req,res through this end point
localhost:27017/api/users

now to make a user router user/userRouter

import express from "express";

const userRouter = express.Router();

export default userRouter

to Create user we need to add this feature
          //validation
          //process
          //response